{% extends 'back.html.twig' %}

{% block title %}Publication{% endblock %}

{% block body %}
  




     <div >
                        <div class="card">
                            <div class="card-body">
                                <div class="mb-4">
                                   <div> 
                                    <h4 class="card-title">La Publication du </h4>
                                    <h6>{{ publication.datePub ? publication.datePub|date('Y-m-d') : ''}}</h6>
                                    </div>
                                    <img src="{{ asset('photos/' ~ publication.image) }}" alt="{{ publication.titre }}">
                                    <p>{{ publication.texte }}</p>
                                 
                                      <div class="col-xl-6" >
                        <div class="card"style="  background-color: #D3D3D3;">
                            <div class="card-body">
                                <div class="mb-4">
                                    <h6 class="card-title">Commentaires</h6>
                                    <p>
                                   {% for commentaire in publication.commentaires %}
                                 <div>{{ commentaire.sujCom }}</div>
                                    {% endfor %}
                                    </p>
                                </div>

                                
                            </div>
                        </div>
                    </div>
                                </div>
<div>
    <a href="{{ path('app_publication_edit', {'idPub': publication.idPub}) }}">edit</a>

    {{ include('publication/_delete_form.html.twig') }}
                          </div> 
                            </div>
                        </div>
                    </div>
{% endblock %}
